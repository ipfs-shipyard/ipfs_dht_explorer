<li class="nav-item dropdown">
  <a class="nav-link dropdown-toggle <%= 'active' if (params[:sort].present? || params[:order].present?) %>" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Sort</a>
  <div class="dropdown-menu dropdown-menu-right text-right">
    <% if params[:sort] != 'updated_at' && params[:order] != 'asc' %>
      <a class="dropdown-item active" title='Click to Remove' href="<%= url_for(request.params.merge(page: nil, sort: nil, order: nil)) %>">
        Newest
      </a>
    <% else %>
      <a class="dropdown-item" href="<%= url_for(request.params.merge(page: nil, sort: 'created_at', order: 'desc')) %>">
        Newest
      </a>
    <% end %>
    <% if params[:sort] == 'created_at' && params[:order] == 'asc' %>
      <a class="dropdown-item active" title='Click to Remove' href="<%= url_for(request.params.merge(page: nil, sort: nil, order: nil)) %>">
        Oldest
      </a>
    <% else %>
      <a class="dropdown-item" href="<%= url_for(request.params.merge(page: nil, sort: 'created_at', order: 'asc')) %>">
        Oldest
      </a>
    <% end %>

    <% if params[:sort] == 'records' && params[:order] == 'asc' %>
      <a class="dropdown-item active" title='Click to Remove' href="<%= url_for(request.params.merge(page: nil, sort: nil, order: nil)) %>">
        Most records
      </a>
    <% else %>
      <a class="dropdown-item" href="<%= url_for(request.params.merge(page: nil, sort: 'records', order: 'desc')) %>">
        Most records
      </a>
    <% end %>
    <% if params[:sort] == 'records' && params[:order] == 'asc' %>
      <a class="dropdown-item active" title='Click to Remove' href="<%= url_for(request.params.merge(page: nil, sort: nil, order: nil)) %>">
        Least records
      </a>
    <% else %>
      <a class="dropdown-item" href="<%= url_for(request.params.merge(page: nil, sort: 'records', order: 'asc')) %>">
        Least records
      </a>
    <% end %>

    <% if params[:sort] == 'size' && params[:order] == 'asc' %>
      <a class="dropdown-item active" title='Click to Remove' href="<%= url_for(request.params.merge(page: nil, sort: nil, order: nil)) %>">
        Largest First
      </a>
    <% else %>
      <a class="dropdown-item" href="<%= url_for(request.params.merge(page: nil, sort: 'size', order: 'desc')) %>">
        Largest First
      </a>
    <% end %>
    <% if params[:sort] == 'size' && params[:order] == 'asc' %>
      <a class="dropdown-item active" title='Click to Remove' href="<%= url_for(request.params.merge(page: nil, sort: nil, order: nil)) %>">
        Smallest first
      </a>
    <% else %>
      <a class="dropdown-item" href="<%= url_for(request.params.merge(page: nil, sort: 'size', order: 'asc')) %>">
        Smallest first
      </a>
    <% end %>
  </div>
</li>
